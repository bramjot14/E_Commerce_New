
## 📘 Learning Logs – E_Commerce_New

### Day-1: Project Setup & Structure

* Set up a full-stack project with separate **frontend** and **backend** folders
* Initialized React frontend and Node/Express backend
* Organized folder structure for scalability
* Configured basic routing and API endpoints

---

### Day-2: Product Data & API Integration

* Created backend APIs to fetch product data
* Connected frontend to backend using fetch/axios
* Rendered product listings dynamically in React
* Learned how frontend and backend communicate in a full-stack app

---

### Day-3: Cart Functionality (Core Logic)

* Implemented cart state using React Context API
* Built `addToCart` functionality with quantity handling
* Implemented `removeFromCart` and item quantity updates
* Calculated cart totals and item counts
* Improved state update logic using array methods (`map`, `filter`, `reduce`)

---

### Day-4: Cart State Persistence & ES6 Modules

* Persisted cart state using `localStorage`
* Loaded initial cart state with `JSON.parse()` on app load
* Synced cart updates using `useEffect`
* Ensured cart data survives page refreshes
* Practiced ES6 default and named imports/exports
* Fixed common import/export syntax issues
* Understood function references vs function calls in React

---

### Day-5: Code Quality & Best Practices

* Followed clean commit conventions (`feat`, `docs`)
* Separated state logic from side effects
* Improved readability and maintainability of cart logic
* Structured commits for better GitHub history
* Learned how to document learning properly using README
